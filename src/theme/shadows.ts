import { ViewStyle } from 'react-native';

export const softShadow: ViewStyle = {
  shadowColor: '#6C3FC5',
  shadowOpacity: 0.08,
  shadowRadius: 20,
  shadowOffset: { width: 0, height: 4 },
  elevation: 3
};
